FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY monitor.py .

# Default dirs (overridden by docker-compose volumes)
RUN mkdir -p /data /reports

EXPOSE 8001

CMD ["uvicorn", "monitor:app", "--host", "0.0.0.0", "--port", "8001"]
